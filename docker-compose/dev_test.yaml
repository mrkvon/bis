version: '3'
services:
  backend:
    environment:
      TEST: 1
      ALLOWED_HOSTS: '*'
      FULL_HOSTNAME: 'http://localhost'
      EMAIL: 'test@maesty.online'

  postgres:
    volumes:
      - ./postgresqldata_test:/var/lib/postgresql/data
